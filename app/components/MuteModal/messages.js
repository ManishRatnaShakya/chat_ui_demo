/*
 * MuteModal Messages
 *
 * This contains all the text for the MuteModal component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MuteModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MuteModal component!',
  },
});
